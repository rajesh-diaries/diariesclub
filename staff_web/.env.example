# Copy to .env.local and fill in. Both values are public (anon/publishable),
# safe to ship to the browser — RLS + RPC guards enforce access server-side.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxx
